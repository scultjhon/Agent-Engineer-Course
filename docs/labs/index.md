# Labs

Labs are deliberately small and portable. Begin with a shell executor, then build a container runner, a tiny agent API, and a Kubernetes Job. Keep secrets outside the repository and document cleanup steps.
