# Lesson 02 — Git and agents

Git gives an agent a durable, reviewable boundary. Use small commits, descriptive messages, branches, and diffs. An agent should inspect repository status before editing and summarize changes after editing.

**Lab:** create `demo-repo`, make one focused change, inspect the diff, and revert only the test change if needed.
