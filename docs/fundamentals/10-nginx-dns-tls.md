# Lesson 10 — Nginx, DNS, and TLS

DNS maps names to endpoints; TLS authenticates and encrypts connections; a reverse proxy routes requests and can enforce headers, limits, and timeouts. Keep certificates and private keys out of Git.

**Lab:** place a local service behind a reverse proxy, add security headers, and test timeout behavior.
