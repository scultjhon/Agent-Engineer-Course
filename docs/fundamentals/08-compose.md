# Lesson 08 — Docker Compose

Compose is useful for local multi-service experiments: an agent API, a mock tool service, and a database. Define health checks, networks, volumes, environment contracts, and cleanup behavior.

**Lab:** create a two-service demo with a health check and a retrying client.
