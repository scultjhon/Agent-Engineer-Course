# Lesson 04 — Shell and runtime

Shell commands are an API with quoting, environment, exit codes, streams, and working-directory semantics. A robust runner validates arguments, uses timeouts, captures stdout/stderr separately, and reports exit status.

**Lab:** build a runner for `sample-app` that rejects unapproved commands and limits output size.
