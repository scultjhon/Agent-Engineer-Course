# Lesson 09 — HTTP, SSE, and WebSocket

HTTP request/response is simple and cacheable. SSE streams server-to-client events over HTTP. WebSocket supports bidirectional messages but adds lifecycle and reconnect complexity. Every protocol needs authentication, timeouts, backpressure, and event IDs.

**Lab:** stream agent events over SSE and reconnect using the last event ID.
