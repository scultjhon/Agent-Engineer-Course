# Codex — public architecture study

Study the public repository by tracing CLI entry points into session orchestration, model streaming, tool execution, approval boundaries, and persistence. Separate observations from inference, and record commit links beside notes.

Suggested questions: Where is the event loop? How are tool calls represented? Which operations require approval? How are subprocesses isolated and terminated?
