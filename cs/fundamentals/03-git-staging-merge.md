# Lesson 03 — Staging, merge, and conflict

The working tree, index, and commit history are different states. A merge conflict is a request for a human or policy decision, not a prompt to overwrite text blindly.

**Lab:** create two branches that edit the same line, merge them, resolve deliberately, run tests, and record why the final version is correct.
